name: send_ip_email
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.17
  http: ^1.2.2
  flutter_local_notifications: ^18.0.1   # Обновление до последней версии
  flutter_local_notifications_linux: ^5.0.0
  flutter_local_notifications_platform_interface: ^8.0.0
  android_alarm_manager_plus: ^4.0.6      # Проверьте, доступна ли новая версия
  permission_handler: ^11.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1
  flutter_background: ^1.3.0

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"  # Путь к иконке
